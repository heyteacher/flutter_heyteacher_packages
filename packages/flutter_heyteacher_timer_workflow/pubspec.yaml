name: flutter_heyteacher_timer_workflow
description: "The flutter heyteacher package for timer workflow"
version: 2.0.0+126
homepage:
publish_to: none # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_heyteacher_locale:
    path: ../flutter_heyteacher_locale
  flutter_heyteacher_platform:
    path: ../flutter_heyteacher_platform

dev_dependencies:
  fake_async: ^1.3.3
  flutter_heyteacher_meta: ^4.4.4+128
  flutter_test:
    sdk: flutter
  logging: ^1.3.0

flutter:
  generate: true

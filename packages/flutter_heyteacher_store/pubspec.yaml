name: flutter_heyteacher_store
description: "A heyteacher flutter package for genetics firestore implementation"
version: 3.1.0+1046
repository: https://codeberg.org/heyteacher/flutter_heyteacher_packages/src/branch/main/packages/flutter_heyteacher_store
license: BSD 3-Clause
issue_tracker: https://codeberg.org/heyteacher/flutter_heyteacher_packages/issues
homepage: https://codeberg.org/heyteacher/flutter_heyteacher_packages/src/branch/main/packages/flutter_heyteacher_store
documentation: https://codeberg.org/heyteacher/flutter_heyteacher_packages/src/branch/main/packages/flutter_heyteacher_store
topics: [firestore, firebase, flutter]
funding: [https://liberapay.com/heyteacher]

environment:
  sdk: ^3.6.2
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  clock: ^1.1.2
  cloud_firestore: ^6.6.0
  collection: ^1.19.1
  copy_with_extension: ^15.0.1
  equatable: ^2.0.8
  firebase_auth: ^6.5.4
  flutter:
    sdk: flutter
  flutter_heyteacher_auth: ^4.0.0+161
  flutter_heyteacher_connectivity: ^3.0.2+147 
  flutter_heyteacher_e2ee: ^3.0.8+159 
  flutter_heyteacher_firebase: ^2.0.11+147 
  flutter_heyteacher_platform: ^3.0.4+148 

  intl: ^0.20.2
  logging: ^1.3.0
  mockito: ^5.6.4
  shared_preferences: ^2.5.5
  synchronized: ^3.4.1

dev_dependencies:
  build_runner: ^2.15.0
  copy_with_extension_gen: ^15.0.1
  fake_cloud_firestore: ^4.1.1
  flutter_heyteacher_meta: ^7.7.0+179
  flutter_secure_storage: ^10.3.1
  flutter_test:
    sdk: flutter
  package_info_plus: ^10.2.0
  shared_preferences_platform_interface: ^2.4.2
  very_good_analysis: ^10.3.0
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true
  uses-material-design: true

  # To add assets to your package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package

name: flutter_heyteacher_platform
description: "The flutter heyteacher package for plaftorm utilities."
version: 2.0.2+130
homepage:
publish_to: none # Remove this line if you wish to publish to pub.dev

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  clock: ^1.1.2
  device_info_plus: ^12.3.0
  flutter:
    sdk: flutter
  flutter_heyteacher_auth:
    path: ../flutter_heyteacher_auth
  flutter_heyteacher_connectivity:
    path: ../flutter_heyteacher_connectivity
  flutter_heyteacher_firebase:
    path: ../flutter_heyteacher_firebase
  flutter_heyteacher_locale:
    path: ../flutter_heyteacher_locale
  flutter_heyteacher_logger:
    path: ../flutter_heyteacher_logger
  flutter_heyteacher_views:
    path: ../flutter_heyteacher_views
  logging: ^1.3.0
  package_info_plus: ^9.0.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_heyteacher_meta: ^4.4.4+128
  flutter_test:
    sdk: flutter

flutter:
  generate: true
